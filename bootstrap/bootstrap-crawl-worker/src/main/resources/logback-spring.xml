<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="APPLICATION_NAME" source="spring.application.name" defaultValue="crawlinghub-crawl-worker"/>

    <!-- ========================================
         JSON + Sentry for Production/Staging
         ======================================== -->
    <springProfile name="prod,staging">
        <!-- JSON Console Appender -->
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <!-- MDC 필드 포함 -->
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>spanId</includeMdcKeyName>
                <includeMdcKeyName>taskId</includeMdcKeyName>
                <includeMdcKeyName>siteName</includeMdcKeyName>
                <includeMdcKeyName>messageId</includeMdcKeyName>

                <!-- 커스텀 필드 -->
                <customFields>{"application":"${APPLICATION_NAME}","environment":"${SPRING_PROFILES_ACTIVE:-prod}"}</customFields>

                <!-- 타임스탬프 포맷 (ISO 8601) -->
                <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSSZ</timestampPattern>

                <!-- 스택 트레이스 설정 -->
                <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                    <maxDepthPerThrowable>30</maxDepthPerThrowable>
                    <maxLength>2048</maxLength>
                    <shortenedClassNameLength>20</shortenedClassNameLength>
                    <rootCauseFirst>true</rootCauseFirst>
                </throwableConverter>
            </encoder>
        </appender>

        <!-- Sentry Appender - ERROR level only -->
        <appender name="SENTRY" class="io.sentry.logback.SentryAppender">
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>ERROR</level>
            </filter>
            <!-- Breadcrumbs: INFO 이상 로그를 Sentry 이벤트 컨텍스트로 포함 -->
            <minimumBreadcrumbLevel>INFO</minimumBreadcrumbLevel>
            <minimumEventLevel>ERROR</minimumEventLevel>
        </appender>

        <!-- Application Loggers -->
        <logger name="com.ryuqq.crawlinghub" level="INFO" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="SENTRY"/>
        </logger>

        <!-- SQS Listener -->
        <logger name="io.awspring.cloud.sqs" level="INFO" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="SENTRY"/>
        </logger>

        <!-- Spring Framework -->
        <logger name="org.springframework" level="WARN" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="SENTRY"/>
        </logger>

        <!-- Hibernate -->
        <logger name="org.hibernate" level="WARN" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="SENTRY"/>
        </logger>

        <!-- HTTP Client -->
        <logger name="org.apache.http" level="WARN" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="SENTRY"/>
        </logger>

        <!-- Root Logger -->
        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="SENTRY"/>
        </root>
    </springProfile>

    <!-- ========================================
         Console Pattern for Local/Dev/Test
         ======================================== -->
    <springProfile name="local,dev,test">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] [%X{traceId:-}/%X{spanId:-}] [%X{siteName:-}/%X{taskId:-}] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <!-- Application Loggers -->
        <logger name="com.ryuqq.crawlinghub" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>

        <!-- SQS Listener -->
        <logger name="io.awspring.cloud.sqs" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>

        <!-- Spring Framework -->
        <logger name="org.springframework" level="INFO" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>

        <!-- Hibernate SQL -->
        <logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>

        <!-- Root Logger -->
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>
