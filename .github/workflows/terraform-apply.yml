name: Terraform Apply

# 아틀란티스로 인프라 관리를 전환하여 자동 트리거 비활성화
# 긴급 상황 시 수동으로만 실행 가능
on:
  workflow_dispatch:
    inputs:
      reason:
        description: '수동 실행 사유 (긴급 상황에만 사용)'
        required: true
        type: string
      module:
        description: 'Terraform module to apply (leave empty for all)'
        required: false
        default: ''
        type: choice
        options:
          - ''
          - ecr
          - ecs-cluster
          - elasticache
          - sqs
          - ecs-web-api
          - ecs-scheduler
          - ecs-crawl-worker
          - eventbridge

  # 자동 트리거 비활성화 (아틀란티스 사용)
  # push:
  #   branches:
  #     - main
  #   paths:
  #     - 'terraform/**'
  #     - '.github/workflows/terraform-*.yml'

permissions:
  contents: read
  id-token: write

jobs:
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      max-parallel: 1
      matrix:
        module:
          - name: ecr
            dir: terraform/ecr
            order: 1
          - name: ecs-cluster
            dir: terraform/ecs-cluster
            order: 2
          - name: elasticache
            dir: terraform/elasticache
            order: 3
          - name: sqs
            dir: terraform/sqs
            order: 4
          - name: ecs-web-api
            dir: terraform/ecs-web-api
            order: 5
          - name: ecs-scheduler
            dir: terraform/ecs-scheduler
            order: 6
          - name: ecs-crawl-worker
            dir: terraform/ecs-crawl-worker
            order: 7
          - name: eventbridge
            dir: terraform/eventbridge
            order: 8

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-northeast-2
          role-duration-seconds: 3600
          role-session-name: GitHubActions-CrawlingHub-TerraformApply

      - name: Terraform Init - ${{ matrix.module.name }}
        working-directory: ${{ matrix.module.dir }}
        run: terraform init

      - name: Check and Import Existing Resources - ${{ matrix.module.name }}
        working-directory: ${{ matrix.module.dir }}
        run: |
          echo "🔍 기존 리소스 확인 및 Import 중..."

          # ECR 모듈: Plan 전에 미리 Import 시도
          if [ "${{ matrix.module.name }}" == "ecr" ]; then
            echo "📦 ECR Repository 존재 여부 확인 및 Import..."

            # web-api ECR
            if aws ecr describe-repositories --repository-names crawlinghub-web-api-prod --region ap-northeast-2 >/dev/null 2>&1; then
              echo "  → crawlinghub-web-api-prod 존재함. Import 시도..."
              terraform import -input=false 'module.ecr_web_api.aws_ecr_repository.this' crawlinghub-web-api-prod 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # scheduler ECR
            if aws ecr describe-repositories --repository-names crawlinghub-scheduler-prod --region ap-northeast-2 >/dev/null 2>&1; then
              echo "  → crawlinghub-scheduler-prod 존재함. Import 시도..."
              terraform import -input=false 'module.ecr_scheduler.aws_ecr_repository.this' crawlinghub-scheduler-prod 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # crawl-worker ECR
            if aws ecr describe-repositories --repository-names crawlinghub-crawl-worker-prod --region ap-northeast-2 >/dev/null 2>&1; then
              echo "  → crawlinghub-crawl-worker-prod 존재함. Import 시도..."
              terraform import -input=false 'module.ecr_crawl_worker.aws_ecr_repository.this' crawlinghub-crawl-worker-prod 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            echo "✅ ECR Import 체크 완료"
          fi

          # ECS-WEB-API 모듈: 기존 리소스 Import
          if [ "${{ matrix.module.name }}" == "ecs-web-api" ]; then
            echo "🌐 ECS Web-API 기존 리소스 확인 및 Import..."

            # ALB Security Group
            ALB_SG_ID=$(aws ec2 describe-security-groups --filters "Name=group-name,Values=crawlinghub-alb-sg-prod" --query 'SecurityGroups[0].GroupId' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$ALB_SG_ID" != "None" ] && [ -n "$ALB_SG_ID" ]; then
              echo "  → ALB Security Group 존재함 (${ALB_SG_ID}). Import 시도..."
              terraform import -input=false 'module.alb_security_group.aws_security_group.this' "$ALB_SG_ID" || echo "  ⚠️  Already in state or import skipped"
            fi

            # ECS Security Group
            ECS_SG_ID=$(aws ec2 describe-security-groups --filters "Name=group-name,Values=crawlinghub-web-api-sg-prod" --query 'SecurityGroups[0].GroupId' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$ECS_SG_ID" != "None" ] && [ -n "$ECS_SG_ID" ]; then
              echo "  → ECS Security Group 존재함 (${ECS_SG_ID}). Import 시도..."
              terraform import -input=false 'module.ecs_security_group.aws_security_group.this' "$ECS_SG_ID" || echo "  ⚠️  Already in state or import skipped"
            fi

            # Target Group
            TG_ARN=$(aws elbv2 describe-target-groups --names crawlinghub-alb-prod-web-api --query 'TargetGroups[0].TargetGroupArn' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$TG_ARN" != "None" ] && [ -n "$TG_ARN" ]; then
              echo "  → Target Group 존재함. Import 시도..."
              terraform import -input=false 'module.alb.aws_lb_target_group.this["web-api"]' "$TG_ARN" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # ALB
            ALB_ARN=$(aws elbv2 describe-load-balancers --names crawlinghub-alb-prod --query 'LoadBalancers[0].LoadBalancerArn' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$ALB_ARN" != "None" ] && [ -n "$ALB_ARN" ]; then
              echo "  → ALB 존재함. Import 시도..."
              terraform import -input=false 'module.alb.aws_lb.this[0]' "$ALB_ARN" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"

              # HTTPS Listener
              HTTPS_LISTENER_ARN=$(aws elbv2 describe-listeners --load-balancer-arn "$ALB_ARN" --query 'Listeners[?Port==`443`].ListenerArn' --output text --region ap-northeast-2 2>/dev/null || echo "None")
              if [ "$HTTPS_LISTENER_ARN" != "None" ] && [ -n "$HTTPS_LISTENER_ARN" ]; then
                echo "  → HTTPS Listener 존재함. Import 시도..."
                terraform import -input=false 'module.alb.aws_lb_listener.https["https"]' "$HTTPS_LISTENER_ARN" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
              fi

              # HTTP Listener
              HTTP_LISTENER_ARN=$(aws elbv2 describe-listeners --load-balancer-arn "$ALB_ARN" --query 'Listeners[?Port==`80`].ListenerArn' --output text --region ap-northeast-2 2>/dev/null || echo "None")
              if [ "$HTTP_LISTENER_ARN" != "None" ] && [ -n "$HTTP_LISTENER_ARN" ]; then
                echo "  → HTTP Listener 존재함. Import 시도..."
                terraform import -input=false 'module.alb.aws_lb_listener.http["http-redirect"]' "$HTTP_LISTENER_ARN" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
              fi
            fi

            # IAM Roles
            if aws iam get-role --role-name crawlinghub-web-api-execution-role-prod >/dev/null 2>&1; then
              echo "  → Execution Role 존재함. Import 시도..."
              terraform import -input=false 'module.ecs_task_execution_role.aws_iam_role.this[0]' crawlinghub-web-api-execution-role-prod 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            if aws iam get-role --role-name crawlinghub-web-api-task-role-prod >/dev/null 2>&1; then
              echo "  → Task Role 존재함. Import 시도..."
              terraform import -input=false 'module.ecs_task_role.aws_iam_role.this[0]' crawlinghub-web-api-task-role-prod 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # CloudWatch Log Group
            if aws logs describe-log-groups --log-group-name-prefix "/aws/ecs/crawlinghub-web-api-prod/application" --query 'logGroups[0].logGroupName' --output text --region ap-northeast-2 2>/dev/null | grep -q "crawlinghub-web-api-prod"; then
              echo "  → CloudWatch Log Group 존재함. Import 시도..."
              terraform import -input=false 'module.web_api_logs.aws_cloudwatch_log_group.this[0]' "/aws/ecs/crawlinghub-web-api-prod/application" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # ECS Service
            if aws ecs describe-services --cluster crawlinghub-cluster-prod --services crawlinghub-web-api-prod --query 'services[0].serviceName' --output text --region ap-northeast-2 2>/dev/null | grep -q "crawlinghub-web-api-prod"; then
              echo "  → ECS Service 존재함. Import 시도..."
              terraform import -input=false 'aws_ecs_service.web_api' "crawlinghub-cluster-prod/crawlinghub-web-api-prod" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # Route53 Record
            ZONE_ID=$(aws route53 list-hosted-zones-by-name --dns-name "set-of.com" --query 'HostedZones[0].Id' --output text 2>/dev/null | sed 's|/hostedzone/||')
            if [ -n "$ZONE_ID" ] && [ "$ZONE_ID" != "None" ]; then
              if aws route53 list-resource-record-sets --hosted-zone-id "$ZONE_ID" --query "ResourceRecordSets[?Name=='crawler.set-of.com.']" --output text 2>/dev/null | grep -q "crawler.set-of.com"; then
                echo "  → Route53 Record 존재함. Import 시도..."
                terraform import -input=false 'aws_route53_record.web_api' "${ZONE_ID}_crawler.set-of.com_A" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
              fi
            fi

            echo "✅ ECS Web-API Import 체크 완료"
          fi

          # ECS-SCHEDULER 모듈: 기존 리소스 Import
          if [ "${{ matrix.module.name }}" == "ecs-scheduler" ]; then
            echo "⏰ ECS Scheduler 기존 리소스 확인 및 Import..."

            # Scheduler Security Group
            SCHEDULER_SG_ID=$(aws ec2 describe-security-groups --filters "Name=group-name,Values=crawlinghub-scheduler-sg-prod" --query 'SecurityGroups[0].GroupId' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$SCHEDULER_SG_ID" != "None" ] && [ -n "$SCHEDULER_SG_ID" ]; then
              echo "  → Scheduler Security Group 존재함 (${SCHEDULER_SG_ID}). Import 시도..."
              terraform import -input=false 'module.ecs_security_group.aws_security_group.this' "$SCHEDULER_SG_ID" || echo "  ⚠️  Already in state or import skipped"
            fi

            # IAM Roles
            if aws iam get-role --role-name crawlinghub-scheduler-execution-role-prod >/dev/null 2>&1; then
              echo "  → Scheduler Execution Role 존재함. Import 시도..."
              terraform import -input=false 'module.scheduler_task_execution_role.aws_iam_role.this[0]' crawlinghub-scheduler-execution-role-prod 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            if aws iam get-role --role-name crawlinghub-scheduler-task-role-prod >/dev/null 2>&1; then
              echo "  → Scheduler Task Role 존재함. Import 시도..."
              terraform import -input=false 'module.scheduler_task_role.aws_iam_role.this[0]' crawlinghub-scheduler-task-role-prod 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # CloudWatch Log Group
            if aws logs describe-log-groups --log-group-name-prefix "/aws/ecs/crawlinghub-scheduler-prod/application" --query 'logGroups[0].logGroupName' --output text --region ap-northeast-2 2>/dev/null | grep -q "crawlinghub-scheduler-prod"; then
              echo "  → CloudWatch Log Group 존재함. Import 시도..."
              terraform import -input=false 'module.scheduler_logs.aws_cloudwatch_log_group.this[0]' "/aws/ecs/crawlinghub-scheduler-prod/application" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # ECS Service
            if aws ecs describe-services --cluster crawlinghub-cluster-prod --services crawlinghub-scheduler-prod --query 'services[0].serviceName' --output text --region ap-northeast-2 2>/dev/null | grep -q "crawlinghub-scheduler-prod"; then
              echo "  → ECS Scheduler Service 존재함. Import 시도..."
              terraform import -input=false 'aws_ecs_service.scheduler' "crawlinghub-cluster-prod/crawlinghub-scheduler-prod" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            echo "✅ ECS Scheduler Import 체크 완료"
          fi

          # ECS-CRAWL-WORKER 모듈: 기존 리소스 Import
          if [ "${{ matrix.module.name }}" == "ecs-crawl-worker" ]; then
            echo "🕷️ ECS Crawl-Worker 기존 리소스 확인 및 Import..."

            # Crawl-Worker Security Group
            WORKER_SG_ID=$(aws ec2 describe-security-groups --filters "Name=group-name,Values=crawlinghub-crawl-worker-sg-prod" --query 'SecurityGroups[0].GroupId' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$WORKER_SG_ID" != "None" ] && [ -n "$WORKER_SG_ID" ]; then
              echo "  → Crawl-Worker Security Group 존재함 (${WORKER_SG_ID}). Import 시도..."
              terraform import -input=false 'module.ecs_security_group.aws_security_group.this' "$WORKER_SG_ID" || echo "  ⚠️  Already in state or import skipped"
            fi

            # IAM Roles
            if aws iam get-role --role-name crawlinghub-crawl-worker-execution-role-prod >/dev/null 2>&1; then
              echo "  → Crawl-Worker Execution Role 존재함. Import 시도..."
              terraform import -input=false 'module.crawl_worker_task_execution_role.aws_iam_role.this[0]' crawlinghub-crawl-worker-execution-role-prod 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            if aws iam get-role --role-name crawlinghub-crawl-worker-task-role-prod >/dev/null 2>&1; then
              echo "  → Crawl-Worker Task Role 존재함. Import 시도..."
              terraform import -input=false 'module.crawl_worker_task_role.aws_iam_role.this[0]' crawlinghub-crawl-worker-task-role-prod 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # CloudWatch Log Group
            if aws logs describe-log-groups --log-group-name-prefix "/aws/ecs/crawlinghub-crawl-worker-prod/application" --query 'logGroups[0].logGroupName' --output text --region ap-northeast-2 2>/dev/null | grep -q "crawlinghub-crawl-worker-prod"; then
              echo "  → CloudWatch Log Group 존재함. Import 시도..."
              terraform import -input=false 'module.crawl_worker_logs.aws_cloudwatch_log_group.this[0]' "/aws/ecs/crawlinghub-crawl-worker-prod/application" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # ECS Service
            if aws ecs describe-services --cluster crawlinghub-cluster-prod --services crawlinghub-crawl-worker-prod --query 'services[0].serviceName' --output text --region ap-northeast-2 2>/dev/null | grep -q "crawlinghub-crawl-worker-prod"; then
              echo "  → ECS Crawl-Worker Service 존재함. Import 시도..."
              terraform import -input=false 'aws_ecs_service.crawl_worker' "crawlinghub-cluster-prod/crawlinghub-crawl-worker-prod" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            echo "✅ ECS Crawl-Worker Import 체크 완료"
          fi

          # SQS 모듈: 기존 리소스 Import
          if [ "${{ matrix.module.name }}" == "sqs" ]; then
            echo "📨 SQS 기존 리소스 확인 및 Import..."

            # KMS Key for SQS
            KMS_KEY_ID=$(aws kms describe-key --key-id alias/crawlinghub-sqs-prod --query 'KeyMetadata.KeyId' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$KMS_KEY_ID" != "None" ] && [ -n "$KMS_KEY_ID" ]; then
              echo "  → SQS KMS Key 존재함 (${KMS_KEY_ID}). Import 시도..."
              terraform import -input=false 'aws_kms_key.sqs' "$KMS_KEY_ID" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
              terraform import -input=false 'aws_kms_alias.sqs' "alias/crawlinghub-sqs-prod" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # SQS Queues - Crawling Task
            CRAWL_TASK_QUEUE_URL=$(aws sqs get-queue-url --queue-name crawlinghub-crawling-task-prod --query 'QueueUrl' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$CRAWL_TASK_QUEUE_URL" != "None" ] && [ -n "$CRAWL_TASK_QUEUE_URL" ]; then
              echo "  → Crawling Task Queue 존재함. Import 시도..."
              terraform import -input=false 'module.crawling_task_queue.aws_sqs_queue.this' "$CRAWL_TASK_QUEUE_URL" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            CRAWL_TASK_DLQ_URL=$(aws sqs get-queue-url --queue-name crawlinghub-crawling-task-prod-dlq --query 'QueueUrl' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$CRAWL_TASK_DLQ_URL" != "None" ] && [ -n "$CRAWL_TASK_DLQ_URL" ]; then
              echo "  → Crawling Task DLQ 존재함. Import 시도..."
              terraform import -input=false 'module.crawling_task_queue.aws_sqs_queue.dlq[0]' "$CRAWL_TASK_DLQ_URL" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # SQS Queues - EventBridge Trigger
            EB_TRIGGER_QUEUE_URL=$(aws sqs get-queue-url --queue-name crawlinghub-eventbridge-trigger-prod --query 'QueueUrl' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$EB_TRIGGER_QUEUE_URL" != "None" ] && [ -n "$EB_TRIGGER_QUEUE_URL" ]; then
              echo "  → EventBridge Trigger Queue 존재함. Import 시도..."
              terraform import -input=false 'module.eventbridge_trigger_queue.aws_sqs_queue.this' "$EB_TRIGGER_QUEUE_URL" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            EB_TRIGGER_DLQ_URL=$(aws sqs get-queue-url --queue-name crawlinghub-eventbridge-trigger-prod-dlq --query 'QueueUrl' --output text --region ap-northeast-2 2>/dev/null || echo "None")
            if [ "$EB_TRIGGER_DLQ_URL" != "None" ] && [ -n "$EB_TRIGGER_DLQ_URL" ]; then
              echo "  → EventBridge Trigger DLQ 존재함. Import 시도..."
              terraform import -input=false 'module.eventbridge_trigger_queue.aws_sqs_queue.dlq[0]' "$EB_TRIGGER_DLQ_URL" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            # IAM Policy for SQS
            if aws iam get-policy --policy-arn "arn:aws:iam::646886795421:policy/crawlinghub-sqs-access-prod" >/dev/null 2>&1; then
              echo "  → SQS IAM Policy 존재함. Import 시도..."
              terraform import -input=false 'aws_iam_policy.sqs_access' "arn:aws:iam::646886795421:policy/crawlinghub-sqs-access-prod" 2>/dev/null || echo "  ⚠️  Already in state or import skipped"
            fi

            echo "✅ SQS Import 체크 완료"
          fi

          # Plan 실행
          echo "📋 Terraform Plan 실행 중..."
          terraform plan -out=tfplan

      - name: Terraform Apply - ${{ matrix.module.name }}
        working-directory: ${{ matrix.module.dir }}
        run: |
          echo "🚀 Applying Terraform for ${{ matrix.module.name }}..."
          terraform apply -auto-approve -no-color tfplan
          echo "✅ Apply completed for ${{ matrix.module.name }}"

      - name: Terraform Output - ${{ matrix.module.name }}
        working-directory: ${{ matrix.module.dir }}
        run: |
          echo "📋 Outputs for ${{ matrix.module.name }}:"
          terraform output -json > outputs-${{ matrix.module.name }}.json
          terraform output

      - name: Upload Apply Results - ${{ matrix.module.name }}
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: apply-results-${{ matrix.module.name }}
          path: ${{ matrix.module.dir }}/outputs-${{ matrix.module.name }}.json
          retention-days: 30

  notify-completion:
    name: Notify Deployment Completion
    needs: terraform-apply
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Create Deployment Summary
        run: |
          echo "## 🎉 CrawlingHub Infrastructure Deployment" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Status**: ${{ needs.terraform-apply.result }}" >> $GITHUB_STEP_SUMMARY
          echo "**Environment**: Production" >> $GITHUB_STEP_SUMMARY
          echo "**Deployed by**: @${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Modules Deployed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ECR Repositories (web-api, scheduler, crawl-worker)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ECS Cluster" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ElastiCache Redis" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ SQS Queues (crawling-task, eventbridge-trigger)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ECS Service: web-api" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ECS Service: scheduler" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ECS Service: crawl-worker" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Next Steps**:" >> $GITHUB_STEP_SUMMARY
          echo "1. Verify infrastructure in AWS Console" >> $GITHUB_STEP_SUMMARY
          echo "2. Check CloudWatch logs" >> $GITHUB_STEP_SUMMARY
          echo "3. Test application endpoints" >> $GITHUB_STEP_SUMMARY
