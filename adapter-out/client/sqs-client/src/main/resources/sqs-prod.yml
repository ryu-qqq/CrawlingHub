# ============================================================
# AWS SQS Client 운영 환경 설정
# ============================================================
# 활성화: spring.profiles.active=prod
# ============================================================

sqs:
  region: ap-northeast-2

logging:
  level:
    com.ryuqq.crawlinghub.adapter.out.sqs: INFO
    software.amazon.awssdk: WARN
